cd - сменить каталог
pwd - текущий каталог
mkdir - создать каталог
chmod - изменить права доступа к файлу или каталогу
chown - сменить пользователя
umask - права доступа по умолчанию
touch - создать файл, touch memo{1,2,3,4,5} создать группу файлов с именами от memo1 до memo5
mv - переместить файл, mv abc def переместить abc в def, mv abc ~ переместить в домашний каталог
cp - скопировать файл,  cp -ra /usr/share/doc/bash-completion* /tmp/b/ рекурсивное копирование с сохранением меток времени и прав доступа
rm - удалить файл, rm -f удалит все файл
ln - сильная ссылка, ln -s слабая ссылка
< - направляет содержимое файла в команду
> - направляет стандартный вывод команды в файлы
2> - направляет стандартную ошибку (сообщение об ошибках) в файл
&> - направляет в файл как стандартный вывод, так и стандартную ошибку
>> - направляет в файл вывод команды, добавляя его в конец существующего файла
exit
ps -u запущенные процессы,  ps u, ps ux | less,  ps aux | less
fg - вывод на передний план команды,выполняемой в фоновом режиме
bg - вывод d фон-режим
history - просмотр списка ранее запущенных команд
echo $PATH
alias - alias p='pwd ; ls –CF'; alias rm='rm -i'
set - установка параметров оболочки
type - отображение местоположения команды
date
hostname
locate - найти команду/файл
less - отобразить информацию на странице
cat - отобразить содержимое файла на єкране
man - информация о команде, man printf
ls - список файлов текущего каталога, ls /usr/bin | sort -f | less
ls -l -a -t равносильно ls -lat где -l(длинний список), -а(скритие файли), -t(сортировка по времени)
"-" - команда из одной букви(-l)
"--" - длинная команда(--version)
При однобуквенных параметрах аргумент обычно отделяется пробелом. Если параметр — это целое слово, то аргумент стоит за знаком равенства (=) ls --hide=Desktop
tar -cvf backup.tar /home/shris здесь параметры создают(-c) файл (-f) с именем "backup.tar", который включает в себя все содержимое каталога "home/chris" и его подкаталогов и показывает подробные сообщения (-v) при создании резервной копии. Поскольку текст backup.tar является аргументом для параметра f, аргумент backup.tar должен располагаться сразу за параметром
id
who -uH здесь параметр -u добавляет информацию о времени простоя и ID процесса, а параметр -H запрашивает вывод заголовка
Если  хотите добавить собственные команды или скрипты оболочки, поместите их в каталог bin в своем каталоге home
do, while, case и else
/.bashrc и ./bash_profile

Команда, псевдоним или функция. Если вводимый текст начинается с обычных символов, интерпретатор попытается дополнить его командой, псевдонимом или названием функции.
Переменная. Если вводимый текст начинается со знака доллара ($), интерпретатор завершает текст переменной из текущей оболочки.
Имя пользователя. Если текст, который вы вводите, начинается с тильды (~), оболочка завершает текст именем пользователя. Тогда ~username указывает на домашний каталог пользователя.
Имя хоста. Если вводимый текст начинается с символа «эт» (@), оболочка завершает текст именем хоста, взятым из файла /etc/hosts.

!n — запустить команду под номером n
!! — запустить предыдущую команду
? — запустить команду со строкой, !?dat?

"|" - конвеер, cat /etc/passwd | sort | less  или gunzip < /usr/share/man/man1/grep.1.gz | nroff -c -man | less
";" - последовательное выполнение команд, date ; troff -me verylargedocument | lpr ; date
отправить сообщение, mail -s "Finished the long command" c hris@example.com
"&" -  запуск команды в фоновом режиме, troff -me verylargedocument | lpr &
Две формы подстановки команд — $(command) и `command`
Арифметические выражения $[выражение] и $(выражение)
Подсчета слов для определения количества найденных файлов echo "There are $(ls | wc -w) files in this directory."

Чтобы изменить файлы /etc/profile и /etc/bashrc, необходимо быть суперпользователем. Однако будет эффективнее создать файл /etc/profile.d/custom.sh для добавления общесистемных настроек, а не редактировать эти файлы напрямую. Пользователи могут изменять информацию в файлах $HOME/.bash_profile, $HOME/.bashrc и $HOME/.bash_logout в собственных домашних каталогах

Добавить переменную окружения PATH=$PATH:/getstuff/bin ; export PATH
